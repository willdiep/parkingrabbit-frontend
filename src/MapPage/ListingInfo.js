import React from 'react'

export const ListingInfo = (props) => {

  return (
    <div>
    <h4 onClick={props.goBack}>Back to Parking Listing</h4>
  
    {/* <p>{props.currentListing}</p> */}

  
  
  
  </div>
  )

}

